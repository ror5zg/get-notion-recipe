#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- "$0" "$@"
|#

(ql:quickload :get-notion-recipe :silent t)

(defun main (&rest argv)
  (declare (ignore argv))
  (get-notion-recipe:main)
  (uiop:quit 0))
